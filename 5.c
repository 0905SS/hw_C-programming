//write a program to compare two number by using of ternary operator in c

#include <stdio.h> 
int main() 
{ 
        int x=8, y=4 ; 
        y = ( x>y ? printf("x is greater") : printf("y is greater") ) ; 
}